# Runtime dependencies actually used by the hardened framework.
# (Removed: bertopic, anthropic, plotly, python-dotenv — no longer imported.
#  Narrative Fragmentation uses sklearn K-Means, not BERTopic; attribution is
#  coded manually, not via an LLM.)
sentence-transformers>=2.7.0
scikit-learn>=1.4.0
scipy>=1.11.0
numpy>=1.26.0
pandas>=2.0.0
streamlit>=1.35.0
pytest>=8.0.0
